# ══════════════════════════════════════════════════════════════════════════════
# docker-compose.prod.yml — Production Stack
# LumindAd · Ad Performance Intelligence Platform v1.0
# Author: Elizabeth Díaz Familia · AI Data Scientist · Sustainable Intelligence & BI
#
# Usage:
#   docker-compose -f docker-compose.prod.yml up -d
#   docker-compose -f docker-compose.prod.yml pull && docker-compose -f docker-compose.prod.yml up -d
#
# Notes:
#   - All secrets must be set in .env (never commit .env to git)
#   - Images pulled from AWS ECR (set ECR_REGISTRY in .env)
#   - Nginx handles SSL termination + reverse proxy on :80 / :443
#   - Resource limits enforce production quotas
# ══════════════════════════════════════════════════════════════════════════════

name: lumindad-prod

x-backend-env: &backend-env
  DATABASE_URL:          ${DATABASE_URL}
  REDIS_URL:             ${REDIS_URL}
  CELERY_BROKER_URL:     ${REDIS_URL}/0
  CELERY_RESULT_BACKEND: ${REDIS_URL}/1
  SECRET_KEY:            ${SECRET_KEY}
  JWT_SECRET:            ${JWT_SECRET}
  APP_ENV:               production
  LOG_LEVEL:             info
  PYTHONUNBUFFERED:      "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONPATH:            /app
  MODEL_DIR:             /app/ml/models
  TF_CPP_MIN_LOG_LEVEL:  "3"
  GREEN_AI_ENABLED:      "true"
  ML_SERVICE_URL:        http://ml:8001
  WORKERS:               "4"
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"
  MAX_UPLOAD_SIZE_MB:    "100"
  CHUNK_SIZE:            "50000"

x-healthcheck-prod: &hc-prod
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 30s

x-logging: &logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:

  # ── Nginx — Reverse proxy + SSL termination ─────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: lumindad_nginx_prod
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ssl_certs:/etc/nginx/ssl:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - frontend
      - backend
      - ml
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "wget", "-qO-", "http://localhost:80/health"]
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── Frontend — nginx static server ─────────────────────────────────────────
  frontend:
    image: ${ECR_REGISTRY}/lumindad-frontend:${IMAGE_TAG:-latest}
    container_name: lumindad_frontend_prod
    restart: always
    expose:
      - "80"
    environment:
      VITE_API_URL:         ${VITE_API_URL:-https://lumindad.ai/api}
      VITE_ML_URL:          ${VITE_ML_URL:-https://lumindad.ai/ml}
      VITE_APP_VERSION:     "1.0.0"
      VITE_APP_ENV:         production
      VITE_ENABLE_GREEN_AI: "true"
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "wget", "-qO-", "http://localhost:80/health"]
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          cpus: "0.1"
          memory: 128M
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── Backend — FastAPI ───────────────────────────────────────────────────────
  backend:
    image: ${ECR_REGISTRY}/lumindad-backend:${IMAGE_TAG:-latest}
    container_name: lumindad_backend_prod
    restart: always
    command: ["/entrypoint.sh", "api"]
    expose:
      - "8000"
    volumes:
      - uploads_prod:/app/uploads
      - ml_models_prod:/app/ml/models:ro
    environment:
      <<: *backend-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "curl", "-fs", "http://localhost:8000/health"]
      start_period: 30s
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.25"
          memory: 512M
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── ML Inference ─────────────────────────────────────────────────────────────
  ml:
    image: ${ECR_REGISTRY}/lumindad-ml:${IMAGE_TAG:-latest}
    container_name: lumindad_ml_prod
    restart: always
    expose:
      - "8001"
    volumes:
      - ml_models_prod:/app/models:ro
    environment:
      ML_PORT:              "8001"
      DEFAULT_MODEL:        XGBoost
      GREEN_AI_ENABLED:     "true"
      GREEN_AI_CPU_W:       "95"
      GREEN_AI_PUE:         "1.57"
      GREEN_AI_CARBON_INTENSITY: "0.475"
      TF_CPP_MIN_LOG_LEVEL: "3"
      LOG_LEVEL:            info
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "curl", "-fs", "http://localhost:8001/health"]
      start_period: 45s
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "2.0"
          memory: 4G
        reservations:
          cpus: "0.5"
          memory: 2G
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── Celery Worker ────────────────────────────────────────────────────────────
  worker:
    image: ${ECR_REGISTRY}/lumindad-backend:${IMAGE_TAG:-latest}
    container_name: lumindad_worker_prod
    restart: always
    command: ["/entrypoint.sh", "worker"]
    volumes:
      - uploads_prod:/app/uploads
      - ml_models_prod:/app/ml/models:ro
    environment:
      <<: *backend-env
      CELERY_CONCURRENCY: "4"
      CELERY_LOGLEVEL:    info
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "1.0"
          memory: 2G
        reservations:
          cpus: "0.25"
          memory: 512M
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── PostgreSQL 15 ─────────────────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    container_name: lumindad_postgres_prod
    restart: always
    environment:
      POSTGRES_DB:       ${POSTGRES_DB:-lumindad}
      POSTGRES_USER:     ${POSTGRES_USER:-lumindad}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA:            /var/lib/postgresql/data/pgdata
    expose:
      - "5432"
    volumes:
      - postgres_prod:/var/lib/postgresql/data
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "pg_isready", "-U", "lumindad", "-d", "lumindad"]
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 2G
        reservations:
          cpus: "0.25"
          memory: 512M
    logging: *logging
    networks:
      - lumindad_prod_net

  # ── Redis 7.2 ──────────────────────────────────────────────────────────────
  redis:
    image: redis:7.2-alpine
    container_name: lumindad_redis_prod
    restart: always
    command: >
      redis-server
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --save 60 1000
      --requirepass ${REDIS_PASSWORD}
    expose:
      - "6379"
    volumes:
      - redis_prod:/data
    healthcheck:
      <<: *hc-prod
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging: *logging
    networks:
      - lumindad_prod_net

volumes:
  postgres_prod:  { driver: local }
  redis_prod:     { driver: local }
  uploads_prod:   { driver: local }
  ml_models_prod: { driver: local }
  ssl_certs:      { driver: local }
  nginx_logs:     { driver: local }

networks:
  lumindad_prod_net:
    driver: bridge
    name: lumindad_production
